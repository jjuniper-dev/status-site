<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>AI Project Dashboard — PATH / HAIL</title>
  <link href="https://fonts.googleapis.com/css2?family=IBM+Plex+Mono:wght@400;600&family=IBM+Plex+Sans:wght@300;400;600;700&family=IBM+Plex+Sans+Condensed:wght@700&display=swap" rel="stylesheet">
  <style>
    /* ========== ROOT VARIABLES ========== */
    :root {
      --red: #E8192E;
      --teal: #00C8C8;
      --gold: #F9D030;
      --green: #7CFFB2;
      --blue-light: #5FA8E8;

      --bg: #07090F;
      --bg2: #0D1117;
      --bg3: #131920;
      --surface: #1A2230;

      --border: rgba(255, 255, 255, 0.10);
      --border-bright: rgba(255, 255, 255, 0.22);

      --text: #F0F4FA;
      --text-dim: #B0BFCF;
      --text-faint: #6A7D90;

      --mono: 'IBM Plex Mono', monospace;
      --sans: 'IBM Plex Sans', sans-serif;
      --cond: 'IBM Plex Sans Condensed', sans-serif;
    }

    /* ========== RESET & BASE ========== */
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    html {
      scroll-behavior: smooth;
    }

    body {
      background: linear-gradient(180deg, var(--bg), var(--bg2));
      color: var(--text);
      font-family: var(--sans);
      font-size: 14px;
      line-height: 1.55;
    }

    a {
      color: var(--teal);
      text-decoration: none;
    }

    a:hover {
      text-decoration: underline;
    }

    /* ========== ANIMATIONS ========== */
    @keyframes pulse {
      0%, 100% {
        opacity: 1;
        box-shadow: 0 0 0 0 rgba(0, 200, 200, 0.7);
      }
      50% {
        opacity: 0.8;
        box-shadow: 0 0 0 10px rgba(0, 200, 200, 0);
      }
    }

    @keyframes shimmer {
      0% {
        background-position: -1000px 0;
      }
      100% {
        background-position: 1000px 0;
      }
    }

    @keyframes fade-in {
      from {
        opacity: 0;
        transform: translateY(10px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    /* ========== LAYOUT CONTAINER ========== */
    .layout {
      display: flex;
      flex-direction: column;
      min-height: 100vh;
    }

    /* ========== TOP NAVIGATION ========== */
    .topnav {
      position: sticky;
      top: 0;
      z-index: 1000;
      display: flex;
      justify-content: space-between;
      align-items: center;
      gap: 16px;
      padding: 12px 20px;
      background: rgba(13, 17, 23, 0.96);
      border-bottom: 1px solid var(--border);
      backdrop-filter: blur(6px);
    }

    .topnav-label {
      font-family: var(--mono);
      font-size: 11px;
      letter-spacing: 0.14em;
      text-transform: uppercase;
      color: var(--text-faint);
    }

    .topnav-links {
      display: flex;
      gap: 8px;
      flex-wrap: wrap;
    }

    .topnav-link {
      display: inline-block;
      font-family: var(--mono);
      font-size: 10px;
      letter-spacing: 0.08em;
      text-transform: uppercase;
      padding: 7px 12px;
      border: 1px solid var(--border);
      color: var(--text-dim);
      background: rgba(255, 255, 255, 0.03);
      transition: all 0.15s;
    }

    .topnav-link:hover {
      border-color: var(--teal);
      color: var(--text);
      background: rgba(0, 200, 200, 0.05);
    }

    .topnav-link.active {
      border-color: var(--teal);
      color: var(--teal);
      box-shadow: 0 0 0 1px rgba(0, 200, 200, 0.2) inset;
    }

    /* ========== MAIN CONTENT AREA ========== */
    .dashboard {
      display: grid;
      grid-template-columns: 220px 1fr 280px;
      gap: 20px;
      padding: 20px;
      flex: 1;
      max-width: 1500px;
      margin: 0 auto;
      width: 100%;
    }

    /* ========== LEFT RAIL ========== */
    .left-rail {
      display: flex;
      flex-direction: column;
      gap: 16px;
    }

    .status-orb {
      width: 80px;
      height: 80px;
      border-radius: 50%;
      background: linear-gradient(135deg, rgba(0, 200, 200, 0.3), rgba(0, 200, 200, 0.1));
      border: 2px solid var(--teal);
      display: flex;
      align-items: center;
      justify-content: center;
      animation: pulse 2s infinite;
      margin: 0 auto;
    }

    .status-dot {
      width: 30px;
      height: 30px;
      border-radius: 50%;
      background: var(--teal);
      animation: pulse 2s infinite;
    }

    .rail-label {
      font-family: var(--mono);
      font-size: 10px;
      letter-spacing: 0.12em;
      color: var(--text-faint);
      text-transform: uppercase;
      text-align: center;
      margin-top: -8px;
    }

    .quick-nav {
      border: 1px solid var(--border);
      background: linear-gradient(160deg, var(--bg2), var(--bg3));
      padding: 12px;
      display: flex;
      flex-direction: column;
      gap: 8px;
    }

    .quick-nav a {
      font-family: var(--mono);
      font-size: 11px;
      letter-spacing: 0.08em;
      text-transform: uppercase;
      padding: 8px 10px;
      border: 1px solid var(--border);
      color: var(--text-dim);
      text-align: center;
      transition: all 0.15s;
    }

    .quick-nav a:hover {
      border-color: var(--teal);
      color: var(--teal);
      background: rgba(0, 200, 200, 0.05);
    }

    /* ========== MAIN CONTENT ========== */
    .main-content {
      display: flex;
      flex-direction: column;
      gap: 24px;
      animation: fade-in 0.3s ease-out;
    }

    .breadcrumb {
      font-family: var(--mono);
      font-size: 10px;
      letter-spacing: 0.08em;
      text-transform: uppercase;
      color: var(--text-faint);
      opacity: 0.7;
    }

    .header {
      border-bottom: 2px solid var(--red);
      padding-bottom: 16px;
    }

    .header-eyebrow {
      font-family: var(--mono);
      font-size: 11px;
      letter-spacing: 0.16em;
      text-transform: uppercase;
      color: var(--red);
      margin-bottom: 8px;
    }

    .header-title {
      font-family: var(--cond);
      font-size: 48px;
      font-weight: 700;
      line-height: 1;
      color: var(--text);
      margin-bottom: 8px;
    }

    .header-title span {
      color: var(--teal);
    }

    .header-sub {
      font-size: 14px;
      color: var(--text-dim);
      line-height: 1.6;
    }

    .badges {
      display: flex;
      gap: 8px;
      flex-wrap: wrap;
      margin-top: 12px;
    }

    .badge {
      font-family: var(--mono);
      font-size: 10px;
      letter-spacing: 0.12em;
      text-transform: uppercase;
      padding: 5px 11px;
      border: 1px solid;
      display: inline-block;
    }

    .badge-red {
      border-color: var(--red);
      color: var(--red);
    }

    .badge-teal {
      border-color: var(--teal);
      color: var(--teal);
    }

    .badge-gold {
      border-color: var(--gold);
      color: var(--gold);
    }

    /* ========== POSITIONING STATEMENT ========== */
    .positioning {
      background: linear-gradient(135deg, #0f1d2e 0%, #0d1520 100%);
      border-left: 3px solid var(--teal);
      padding: 18px 20px;
      display: flex;
      align-items: center;
      gap: 16px;
    }

    .positioning-label {
      font-family: var(--mono);
      font-size: 10px;
      color: var(--teal);
      letter-spacing: 0.15em;
      text-transform: uppercase;
      white-space: nowrap;
      writing-mode: vertical-rl;
      transform: rotate(180deg);
    }

    .positioning-text {
      font-family: var(--cond);
      font-size: 20px;
      font-weight: 700;
      line-height: 1.35;
      color: var(--text);
    }

    .positioning-text em {
      color: var(--teal);
      font-style: normal;
    }

    /* ========== SECTION LABEL ========== */
    .section-label {
      font-family: var(--mono);
      font-size: 11px;
      letter-spacing: 0.18em;
      text-transform: uppercase;
      color: var(--text-faint);
      display: flex;
      align-items: center;
      gap: 12px;
      margin: 16px 0 12px;
    }

    .section-label::after {
      content: '';
      flex: 1;
      height: 1px;
      background: var(--border);
    }

    /* ========== GRID LAYOUTS ========== */
    .grid-2 {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 16px;
    }

    .grid-3 {
      display: grid;
      grid-template-columns: 1fr 1fr 1fr;
      gap: 16px;
    }

    .card {
      border: 1px solid var(--border);
      background: linear-gradient(160deg, var(--bg2), var(--bg3));
      padding: 18px 20px;
      transition: all 0.15s;
    }

    .card:hover {
      border-color: var(--teal);
    }

    .card-title {
      font-family: var(--cond);
      font-size: 20px;
      font-weight: 700;
      color: var(--text);
      margin-bottom: 8px;
    }

    .card-sub {
      font-size: 13px;
      color: var(--text-dim);
      line-height: 1.6;
      margin-bottom: 12px;
    }

    .card-list {
      list-style: none;
    }

    .card-list li {
      font-size: 13px;
      color: var(--text-dim);
      padding: 6px 0;
      border-bottom: 1px solid var(--border);
      display: flex;
      gap: 8px;
    }

    .card-list li:last-child {
      border-bottom: none;
    }

    .card-list li::before {
      content: '→';
      color: var(--teal);
      flex-shrink: 0;
    }

    /* ========== METRICS BOX ========== */
    .metrics-box {
      border: 1px solid var(--border);
      background: linear-gradient(160deg, var(--bg2), var(--bg3));
      padding: 14px 16px;
      display: flex;
      gap: 12px;
      align-items: center;
      transition: all 0.15s;
    }

    .metric-label {
      font-family: var(--mono);
      font-size: 10px;
      letter-spacing: 0.12em;
      color: var(--text-faint);
      text-transform: uppercase;
      flex: 1;
    }

    .metric-value {
      font-family: var(--cond);
      font-size: 20px;
      font-weight: 700;
      color: var(--teal);
    }

    /* ========== TABLE ========== */
    .table {
      width: 100%;
      border: 1px solid var(--border);
      border-collapse: collapse;
      margin: 12px 0;
      background: var(--bg2);
    }

    .table th {
      font-family: var(--mono);
      font-size: 10px;
      letter-spacing: 0.12em;
      text-transform: uppercase;
      color: var(--text-dim);
      background: var(--surface);
      padding: 11px 14px;
      text-align: left;
      border-bottom: 1px solid var(--border-bright);
    }

    .table td {
      padding: 11px 14px;
      border-bottom: 1px solid var(--border);
      font-size: 13px;
      color: var(--text-dim);
    }

    .table tr:last-child td {
      border-bottom: none;
    }

    .table tr:hover {
      background: rgba(255, 255, 255, 0.02);
    }

    .status-badge {
      display: inline-flex;
      align-items: center;
      gap: 6px;
      font-size: 12px;
    }

    .status-dot {
      width: 8px;
      height: 8px;
      border-radius: 50%;
      flex-shrink: 0;
    }

    .dot-active {
      background: var(--green);
    }

    .dot-emerging {
      background: var(--gold);
    }

    .dot-conditional {
      background: var(--blue-light);
    }

    .dot-blocked {
      background: var(--red);
    }

    /* ========== RIGHT PANEL ========== */
    .right-panel {
      display: flex;
      flex-direction: column;
      gap: 16px;
      height: fit-content;
      position: sticky;
      top: 80px;
    }

    .panel-card {
      border: 1px solid var(--border);
      background: linear-gradient(160deg, var(--bg2), var(--bg3));
      padding: 14px 16px;
      border-radius: 0;
    }

    .panel-title {
      font-family: var(--cond);
      font-size: 14px;
      font-weight: 700;
      color: var(--text);
      margin-bottom: 10px;
      display: flex;
      align-items: center;
      gap: 8px;
    }

    .panel-title::before {
      content: '';
      width: 6px;
      height: 6px;
      border-radius: 50%;
      background: var(--teal);
      flex-shrink: 0;
    }

    .panel-list {
      list-style: none;
    }

    .panel-list li {
      font-size: 12px;
      color: var(--text-dim);
      padding: 6px 0;
      border-bottom: 1px solid rgba(255, 255, 255, 0.05);
      line-height: 1.5;
    }

    .panel-list li:last-child {
      border-bottom: none;
    }

    /* ========== FOOTER ========== */
    .footer {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 20px;
      border-top: 1px solid var(--border);
      margin-top: 20px;
      background: var(--bg);
    }

    .footer-left, .footer-right {
      font-family: var(--mono);
      font-size: 10px;
      color: var(--text-faint);
      line-height: 1.8;
    }

    .footer-right {
      text-align: right;
    }

    /* ========== RESPONSIVE ========== */
    @media (max-width: 1200px) {
      .dashboard {
        grid-template-columns: 200px 1fr 260px;
        gap: 16px;
        padding: 16px;
      }

      .grid-3 {
        grid-template-columns: 1fr 1fr;
      }

      .header-title {
        font-size: 40px;
      }
    }

    @media (max-width: 980px) {
      .dashboard {
        grid-template-columns: 1fr;
        gap: 20px;
      }

      .right-panel {
        position: static;
        order: 3;
      }

      .grid-2, .grid-3 {
        grid-template-columns: 1fr;
      }

      .positioning {
        flex-direction: column;
        align-items: flex-start;
      }

      .positioning-label {
        writing-mode: horizontal-tb;
        transform: none;
      }
    }

    @media (max-width: 768px) {
      .topnav {
        flex-direction: column;
        align-items: flex-start;
        gap: 12px;
      }

      .topnav-links {
        width: 100%;
        order: 3;
      }

      .header-title {
        font-size: 32px;
      }

      .left-rail {
        display: none;
      }

      .dashboard {
        grid-template-columns: 1fr;
      }

      .right-panel {
        order: 4;
      }
    }

    @media (max-width: 480px) {
      body {
        font-size: 13px;
      }

      .topnav {
        padding: 12px 16px;
      }

      .dashboard {
        padding: 12px 16px;
        gap: 12px;
      }

      .header-title {
        font-size: 24px;
      }

      .card {
        padding: 12px 14px;
      }
    }
  </style>
</head>
<body>
<div class="layout">
  <!-- TOP NAVIGATION -->
  <div class="topnav">
    <div class="topnav-label">AI Project Dashboard</div>
    <div class="topnav-links">
      <a class="topnav-link active" href="index.html">Dashboard</a>
      <a class="topnav-link" href="decisions.html">Decisions</a>
      <a class="topnav-link" href="scenarios.html">Scenarios</a>
      <a class="topnav-link" href="intelligence.html">Intelligence</a>
      <a class="topnav-link" href="artifacts.html">Artifacts</a>
      <a class="topnav-link" href="path-architecture.html">Architecture</a>
      <a class="topnav-link" href="control-plane.html">Control Plane</a>
    </div>
  </div>

  <!-- MAIN DASHBOARD LAYOUT -->
  <div class="dashboard">
    <!-- LEFT RAIL -->
    <div class="left-rail">
      <div class="status-orb">
        <div class="status-dot"></div>
      </div>
      <div class="rail-label">System Active</div>

      <div class="quick-nav">
        <a href="#overview">Overview</a>
        <a href="#decisions">Decisions</a>
        <a href="#scenarios">Scenarios</a>
        <a href="#timeline">Timeline</a>
      </div>
    </div>

    <!-- MAIN CONTENT -->
    <div class="main-content">
      <div class="breadcrumb">Dashboard &gt; PATH / HAIL Status</div>

      <div class="header">
        <div class="header-eyebrow">Enterprise AI — Governance & Capability</div>
        <div class="header-title">PATH <span>/</span> HAIL</div>
        <div class="header-sub">Health Canada · Public Health Agency of Canada</div>
        <div class="badges">
          <div class="badge badge-red">Protected B</div>
          <div class="badge badge-teal">Active</div>
          <div class="badge badge-gold">FY26-27</div>
        </div>
      </div>

      <div class="positioning">
        <div class="positioning-label">Core Position</div>
        <div class="positioning-text">PATH is the <em>control plane</em> for enterprise AI governance. HAIL is the active delivery environment facing a production decision.</div>
      </div>

      <!-- SECTION: STATUS SNAPSHOT -->
      <div id="overview">
        <div class="section-label">Status Snapshot</div>
        <table class="table">
          <thead>
            <tr>
              <th>Area</th>
              <th>Current State</th>
              <th>Status</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td><strong>HAIL Deployment</strong></td>
              <td>Platform deployed, GREP-ExP migrated, key bugs resolved</td>
              <td><span class="status-badge"><span class="status-dot dot-active"></span>Active</span></td>
            </tr>
            <tr>
              <td><strong>HAIL Production Move</strong></td>
              <td>No ATO conversation initiated with CIO</td>
              <td><span class="status-badge"><span class="status-dot dot-blocked"></span>Blocked</span></td>
            </tr>
            <tr>
              <td><strong>HC-PATH</strong></td>
              <td>Pre-prototype, not viable as alternate landing zone</td>
              <td><span class="status-badge"><span class="status-dot dot-emerging"></span>Emerging</span></td>
            </tr>
            <tr>
              <td><strong>FY26-27 Resourcing</strong></td>
              <td>230 MS hours, cloud credits, governance flagged</td>
              <td><span class="status-badge"><span class="status-dot dot-conditional"></span>Conditional</span></td>
            </tr>
          </tbody>
        </table>
      </div>

      <!-- SECTION: KEY INSIGHTS -->
      <div id="decisions">
        <div class="section-label">Key Insights</div>
        <div class="grid-2">
          <div class="card">
            <div class="card-title">Governance Challenge</div>
            <div class="card-sub">HAIL is ready for production but requires ATO approval before FY26-27 can proceed.</div>
            <ul class="card-list">
              <li>ATO gap blocks production path</li>
              <li>Governance escalation required</li>
              <li>May-June 2026 decision window</li>
            </ul>
          </div>
          <div class="card">
            <div class="card-title">Agentic AI Reference</div>
            <div class="card-sub">GREP-ExP demonstrates multi-agent screening with human-in-the-loop governance patterns.</div>
            <ul class="card-list">
              <li>L3/L4 operational capability example</li>
              <li>Published research (Cochrane 2025)</li>
              <li>Governs disagreement-based escalation</li>
            </ul>
          </div>
        </div>
      </div>

      <!-- SECTION: SCENARIOS -->
      <div id="scenarios">
        <div class="section-label">Featured Scenarios</div>
        <div class="grid-2">
          <a href="scenarios.html" class="card" style="text-decoration: none; color: inherit;">
            <div class="card-title">HAIL Production Move</div>
            <div class="card-sub">Path to unblocking FY26-27 delivery and production governance</div>
            <div style="font-size: 12px; color: var(--teal); margin-top: 8px;">→ View scenario</div>
          </a>
          <a href="scenarios.html" class="card" style="text-decoration: none; color: inherit;">
            <div class="card-title">PATH Governance</div>
            <div class="card-sub">Establishing control plane architecture and onboarding model</div>
            <div style="font-size: 12px; color: var(--teal); margin-top: 8px;">→ View scenario</div>
          </a>
        </div>
      </div>

      <!-- SECTION: TIMELINE PREVIEW -->
      <div id="timeline">
        <div class="section-label">FY26-27 Timeline</div>
        <div class="grid-2">
          <div class="card">
            <div class="card-title">April 2026 (Now)</div>
            <ul class="card-list">
              <li>HAIL deployment complete</li>
              <li>GREP-ExP onboarded</li>
              <li>ATO decision pending</li>
            </ul>
          </div>
          <div class="card">
            <div class="card-title">May-Aug 2026</div>
            <ul class="card-list">
              <li>CIO briefing and escalation</li>
              <li>ATO approval target</li>
              <li>Production unblock</li>
            </ul>
          </div>
        </div>
      </div>

      <!-- SECTION: CALL TO ACTION -->
      <div class="positioning" style="margin-top: 20px;">
        <div class="positioning-label">Next</div>
        <div class="positioning-text">Review <em>Decisions</em> log to track governance status, or explore <em>Scenarios</em> for use case context.</div>
      </div>
    </div>

    <!-- RIGHT PANEL -->
    <div class="right-panel">
      <div class="panel-card">
        <div class="panel-title">Active Decisions</div>
        <ul class="panel-list">
          <li><strong>DEC-001:</strong> HAIL Production ATO Gap</li>
          <li><strong>DEC-002:</strong> PATH/HAIL Convergence</li>
          <li><strong>DEC-003:</strong> HC-PATH Pre-Prototype</li>
        </ul>
      </div>

      <div class="panel-card">
        <div class="panel-title">Blockers</div>
        <ul class="panel-list">
          <li>ATO conversation not initiated</li>
          <li>Production path unconfirmed</li>
          <li>FY26-27 budget undefined</li>
        </ul>
      </div>

      <div class="panel-card">
        <div class="panel-title">Next Steps</div>
        <ul class="panel-list">
          <li>Escalate to CIO</li>
          <li>Draft HAIL-to-production ADR</li>
          <li>Schedule governance review</li>
          <li>Confirm FY26-27 resources</li>
        </ul>
      </div>

      <div class="panel-card">
        <div class="panel-title">Quick Links</div>
        <ul class="panel-list">
          <li><a href="decisions.html">→ Decisions Log</a></li>
          <li><a href="scenarios.html">→ Scenarios</a></li>
          <li><a href="intelligence.html">→ Intelligence</a></li>
          <li><a href="artifacts.html">→ Artifacts</a></li>
        </ul>
      </div>
    </div>
  </div>

  <!-- FOOTER -->
  <div class="footer">
    <div class="footer-left">
      Health Canada · Public Health Agency of Canada<br>
      AI Project Dashboard — Internal Use<br>
      <span id="version">Version 1.0 · April 2026</span>
    </div>
    <div class="footer-right">
      <span id="last-updated">Last updated: 2026-04-19</span>
    </div>
  </div>
</div>

<script>
  // Auto-set active nav link based on current page
  document.addEventListener('DOMContentLoaded', function() {
    const currentPage = window.location.pathname.split('/').pop() || 'index.html';
    document.querySelectorAll('.topnav-link').forEach(link => {
      if (link.getAttribute('href') === currentPage) {
        link.classList.add('active');
      } else {
        link.classList.remove('active');
      }
    });
  });
</script>
</body>
</html>
